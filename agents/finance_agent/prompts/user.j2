下面是本次分析全部参考数据，结合用户提问自主判断输出篇幅与结构：
【用户原始提问】
{{ user_query }}

【个人月度预算&储蓄目标】
{{ target_data }}

【当月账单统计汇总】
{{ stat_data }}

【确定性事实底稿 —— 数字白名单（D16 防幻觉）
事实性数字（本月实际支出/余额/超支额/比例/单笔金额）一律严格引用下方数值，禁止改写或编造；底稿未提供的数字不得当作事实。
建议性/推算性数字（节约幅度、预算下调、预计值）可以出现，但必须用"建议/预计/下调至/控制在"等词框定、基于注入数据推算，不得冒充实际发生额。】
{% if fact_sheet %}{{ fact_sheet }}{% else %}（无预算与支出底稿数据，缺失项如实说明，不得推算编造）{% endif %}

【当前单笔消费城市物价对标数据（无则为空）】
{{ price_data }}

【历史消费习惯（近3个月按月聚合：品类/笔数/金额/均额，当月为累计至今；可用于推算剩余天数内预计消费频次与金额，无数据则为空）】
{{ habit_data }}

【用户资料（用户自己记录的笔记检索结果，每条标注来源文档。仅当与本次分析相关时引用，并在给建议时说明依据了哪篇用户笔记；若与预算/消费习惯/目标无关则忽略，不要编造笔记中没有的内容）】
{% if user_docs_data %}
{% for d in user_docs_data %}- （用户笔记《{{ d.doc_id }}》）{{ d.text }}
{% endfor %}{% else %}- （无相关用户笔记）{% endif %}

请严格遵守上方角色与输出规则完成分析，仅输出自然语言分析文本，不要额外说明、注释、标签。