当前用户输入：{{ user_input }}
完整历史对话：
{{ history }}

请按照系统要求输出标准JSON任务规划结果。
【强制】每个任务对象必须完整包含 name、raw_segments、operate_sub_type 三个字段，严禁缺失任何字段。
【强制】分析建议类需求（理财建议/消费分析/如何省钱）必须规划成 finance 任务，严禁规划成 bill。
【强制】只有用户**明确表达记账动作**（"记/记账/记录/记一下/帮我记/写入/入账"等）才能规划 bill 任务；
"花了/买了/用了/付了/消费了"只是开销陈述，不是记账指令。用户仅陈述开销或咨询合理性（"打车花了80块呢"
"吃饭600块正常吗"）且无记账指令时，严禁规划 bill——按咨询处理（问合理性→finance，问比价→price）；
承接上文咨询的"那…呢"沿用上文咨询意图，不规划 bill。
【强制】同一语句同时含记账与理财/比价意图时（如"记一下花了300买耳机，值吗"），finance/price 任务与 bill 任务一样，raw_segments 必须完整携带金额与消费品类（示例："花了300买耳机"），严禁只写"值吗""合理吗"等无参数片段。
【强制】用户明确拒绝记账（"别记这笔""不用记""只是问问"等）时，严禁规划 bill 任务。
【可选】任务对象可带 deps 字段（数组），声明本任务必须等待哪些前置任务完成（值只能是本轮任务的 name，如 ["bill"]）；无依赖或不确定时留空数组 [] 或省略该字段，系统会自动补齐必要依赖，不会因省略而出错。